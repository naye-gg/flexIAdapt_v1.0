{
  "name": "flexiadapt-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && pnpm install && pnpm run build",
    "start": "cd backend && pnpm start",
    "dev": "cd backend && pnpm run dev"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
